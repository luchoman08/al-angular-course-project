import { IndefiniteArticlePipe } from './indefinite-article.pipe';

describe('IndefiniteArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new IndefiniteArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
