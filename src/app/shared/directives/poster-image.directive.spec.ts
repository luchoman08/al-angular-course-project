import { PosterImageDirective } from './poster-image.directive';

describe('PosterImageDirective', () => {
  it('should create an instance', () => {
    const directive = new PosterImageDirective();
    expect(directive).toBeTruthy();
  });
});
